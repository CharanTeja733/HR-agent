# Database Migrations

Placeholder for Alembic migration files. Currently tables are created at startup via raw SQL in `app/database.py`. When the schema grows complex enough to need versioned migrations, Alembic will be configured here.
